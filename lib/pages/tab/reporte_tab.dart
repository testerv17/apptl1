import 'package:flutter/material.dart';

class ReporteTab extends StatelessWidget {
  const ReporteTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('REPORTE TAB'),
      ),
    );
  }
}
